const array = ['this', 'is', 'teja'];

const hasValue = array.includes('teja');

// checking the condition
if(hasValue) {
    console.log('Array contains a value.');
} else {
    console.log('Array does not contain a value.');
}